name: Build Android on Push

on:
  push:
    branches:
      - main

jobs:
  build:
    uses: ./.github/workflows/build-android-e2e.yml
    with:
      build_type: Release
      metamask_environment: qa
